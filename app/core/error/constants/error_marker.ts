/**
 * Unique symbol to mark the error as an exception.
 */
export const EXCEPTION_MARKER: unique symbol = Symbol('@marker/core/error/exception')

/**
 * Unique symbol to mark the error as an internal error.
 */
export const INTERNAL_ERROR_MARKER: unique symbol = Symbol('@marker/core/error/internal')
